# ── Core Framework ──
fastapi
uvicorn[standard]
python-dotenv

# ── LLM & Agents ──
langchain-xai
langchain_tavily
langgraph

# ── TTS ──
edge-tts

# ── HTTP Client ──
requests

# ── File Upload Support ──
python-multipart

# ── Database (shared MongoDB with the Next.js app) ──
pymongo[srv]

# ── Scheduler ──
apscheduler

# ── Push notifications ──
firebase-admin